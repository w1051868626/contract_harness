# 核心依赖（与 pyproject.toml [dependencies] 一致）
# 安装方式：pip install -e . 或 pip install -r requirements.txt
click>=8.1
pydantic>=2.0
httpx>=0.27
openai>=1.0
jinja2>=3.0
python-docx>=1.0
pypdf>=4.0
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
python-dotenv>=1.0
chromadb>=0.5.0
loguru>=0.7
